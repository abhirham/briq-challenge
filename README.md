## Project setup and running
```
Clone the repo.

run "npm install" in both the front-end and back-end folders.

inside back-end folder, run "node server.js" (this will start the server on port 3000)

inside front-end folder, run "npm run serve" (this will host the UI on port 8080)

go to localhost:8080 in browser.
```